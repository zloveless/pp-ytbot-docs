---
layout: ../../layouts/Base.astro
title: Spawn Reference — ytbot
---
import CommandTable from '../../components/CommandTable.astro';
import ZombiePools from '../../components/ZombiePools.astro';

# Spawn Reference

## How Spawning Works

When a spawn command is triggered:

1. The targeted player receives the **auto-pull buff**, which periodically draws aggro from the spawned zombies.
2. Zombies are **randomly chosen** from the command's pool based on configured weights.
3. As **Game Stage** increases, base zombie counts will also increase.

## Extra Zombies via Super Chat

If your Super Chat donation exceeds the command's minimum price, you spawn **extra zombies** on top of the base count.

**Formula:** `extra zombies = floor((donation − minimum) ÷ threshold)`

- `donation` — your Super Chat amount in USD
- `minimum` — the command's Super Chat minimum (see table below)
- `threshold` — the USD increment per extra zombie (see table below)

**Example:** `!spawn dogs` has a $2.00 minimum and a $0.25 threshold.
A $3.00 Super Chat → $1.00 over minimum ÷ $0.25 = **4 extra zombies** on top of the base 32.

> **Screamers** are an exception — they have no base count and no Super Chat scaling. The screamer horde size is determined entirely by your current Game Stage.

## Basic Zombie Spawn Commands

The "posse" spawns are budget-friendly and scale purely with **Game Stage (GS)** — unlike the Super Chat scaling described above, a donation over the minimum does **not** spawn extra zombies for these commands. Remember, harder biomes have a higher GS, so the same command spawns more zombies there than in an easy biome.

<CommandTable category="spawn" variant="posse" />

## Spawn Commands

<div class="alert alert-warning" role="alert"><strong>Heads up:</strong> spawn commands aren't allowed while the streamer is at home base — admins will <code>killall</code> anything called in there. Screamers are the exception: their horde is pulled in via the telnet <code>spawnscouts</code> command, so they can still be called in regardless of location.</div>

<div class="alert alert-success" role="alert"><strong>POI note:</strong> <s>spawns can be finicky inside points of interest — tight interiors often don't have enough open space for a large horde to spawn in, [...]</s> -- Superceded. Spawns will retry up to 20 times before refunding the amount spent back as points.</div>

Click any column header to sort. The **Notes** column references the zombie pool used — see [Zombie Pools](#zombie-pools) below for the full member list.

<CommandTable category="spawn" />

## Boss Spawn Commands

Elite, mini-boss, and boss-tier zombies — the toughest and most expensive spawns.

<CommandTable category="spawn" variant="boss" />

<h3 id="zombie-pools">Zombie Pools</h3>

<ZombiePools />

Charged/infernal variants draw from the same named zombie pool but spawn that variant of each zombie instead.
